# driver-mgt Python dependencies
# Compatible with Python 3.9+ (including Python 3.11 on Debian 12)
#
# Debian 12 Note: Install these in a virtual environment
# PEP 668 compliance - do not install system-wide with pip
#
# Installation: The install.sh script handles this automatically
# Manual install: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

PyQt6>=6.4.0
psutil>=5.9.0
requests>=2.28.0
pyyaml>=6.0
setuptools>=65.0
